## 2. Qual o propósito do YAML do documento?
## R. O YAML é o cabeçalho, ele possui alguns parâmetros pré-definidos que guiarão
## o RStudio a identificar o documento e como proceder nele, usado para gerar o arquivo final
